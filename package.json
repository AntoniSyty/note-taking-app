{
  "name": "note-taking-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --stack-name note-taking-app --capabilities CAPABILITY_IAM --profile antonsyty --resolve-s3",
    "setup": "npm run build && npm run deploy",
    "test": "node ./test"
  },
  "author": "Anton Syty",
  "dependencies": {
    "request": "^2.88.2"
  }
}
